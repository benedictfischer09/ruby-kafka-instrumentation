RSpec.describe Kafka::Instrumentation do
  it "has a version number" do
    expect(Kafka::Instrumentation::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
