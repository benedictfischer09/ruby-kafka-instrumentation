RSpec.describe Ruby::Kafka::Instrumentation do
  it "has a version number" do
    expect(Ruby::Kafka::Instrumentation::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
